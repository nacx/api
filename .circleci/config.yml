version: 2.1

workflows:
  version: 2
  build_and_publish:
    jobs:
      - build:
          filters:
            tags:
              only: /.*/

jobs:
  build:
    machine:
      image: ubuntu-1604:201903-01
    steps:
      # TODO(dio): Execute lint-all.
      - run: make
